<div class="row">
  <div class="col-md-6- col-md-offset-3">
    <div class="well">
      <hi>Look Up Your Local Forecast</hi>
      <div>
        <%= form_tag index_path do %>
        <%= text_field_tag :zipcode, nil, placeholder: "zipcode", class: "form-control" %><br/>
        <%= submit_tag "Check Weather", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-md-6- col-md-offset-3">
    <div class="well">
      <% if @location != nil && @location != "" && (@error == "" || @error == nil) %>

      <h1>Forecast..............</h1>
      <p>
        <em>What's the weather like where you live?</em>
      </p>
      <p>
      Temperature is:
      <%= @temp %>° F
      </p>
      <p>
        CLoud Cover:
        <%= @cloudiness %>%
      </p>
      <p>
        Wind:
        <%= @windiness %>mph
      </p>
      <div class="col-md-5">
        <p>
          <%= @weather_words %>
          <img src="http://openweathermap.org/img/w/<%= @weather_icon%>.png">
        </p>
      </div>
    </div>
    <% elsif @status == "404" %>
    <p>Error:<%= @error %>. Please Try Again.</p>
    <% else %>
    <p>Enter a zip code to check the weather!</p>
    <% end %>
  </div>
  </div>
  </div>
  </div>
